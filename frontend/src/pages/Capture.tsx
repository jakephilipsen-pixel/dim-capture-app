import { PlaceholderPage } from '@/pages/PlaceholderPage'

export default function Capture() {
  return (
    <PlaceholderPage
      route="/"
      title="Capture"
      note="Barcode scan → measure → save flow lands in module 06 (capture-page)."
    />
  )
}
